<?xml version="1.0" encoding="utf-8"?>
<!-- One line of a settings group: what it is, and what it is set to. The group's
     card is the ground; the row only lights when touched. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layoutDirection="locale"
    android:background="@drawable/row_flat"
    android:clickable="true"
    android:focusable="true"
    android:minHeight="52dp"
    android:paddingStart="14dp"
    android:paddingEnd="14dp">

    <TextView
        android:id="@+id/set_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/text"
        android:textSize="15sp" />

    <TextView
        android:id="@+id/set_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/accent"
        android:textSize="14sp"
        android:textStyle="bold" />
</LinearLayout>
